name: travel
description: "Mini Travel Assistant - Final Year CS Project"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.0
  sqflite: ^2.3.0
  path: ^1.8.0
  http: ^1.2.0
  shared_preferences: ^2.2.0
  go_router: ^13.0.0
  json_annotation: ^4.8.0
  connectivity_plus: ^5.0.0
  cached_network_image: ^3.3.0
  flutter_rating_bar: ^4.0.1
  intl: ^0.19.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0

flutter:
  uses-material-design: true
